/* PureDarwin stub: the real Apple header pulls in DriverKit/*.h, which
 * isn't available (or needed) in this kernel-private kext build. USB.h
 * doesn't reference any symbol from it in the subset we actually use. */
#ifndef _IOKIT_USB_IOUSBHOSTFAMILYDEFINITIONS_H
#define _IOKIT_USB_IOUSBHOSTFAMILYDEFINITIONS_H
#endif
